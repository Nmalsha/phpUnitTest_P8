<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link
      rel="icon"
      href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>"
    />
    <link
      rel="stylesheet"
      href="https://bootswatch.com/5/lux/bootstrap.min.css"
    />
    <link
      href="//netdna.bootstrapcdn.com/bootstrap/3.0.0/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/public/style.css" />
    {# Run `composer require symfony/webpack-encore-bundle` to start using
    Symfony UX #} {% block stylesheets %}
    {{
      encore_entry_link_tags("app")
    }}
    {% endblock %} {% block javascripts %}
    {{
      encore_entry_script_tags("app")
    }}
    {% endblock %}
  </head>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">TODO & CO</a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarColor01"
        aria-controls="navbarColor01"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link active" href="{{ path('homepage') }}"
              ><i class="fas fa-home"></i>Home
              <span class="visually-hidden">(current)</span>
            </a>
          </li>
          <li class="nav-item">
            <a href="{{ path('task_create') }}" class="nav-link active"
  >Créer une nouvelle tâche</a
>
          </li>
          <li class="nav-item">
            <a href="{{ path('task_list') }}" class="nav-link active"
  >Consulter la liste des tâches à faire</a
>
          </li>
          <li class="nav-item">
            <a href="{{ path('task_treated') }}" class="nav-link active"
  >Consulter la liste des tâches terminées</a
>
          </li>
          <li class="nav-item">
            <a href="{{ path('app_login') }}" class="nav-link active"
  >Login</a
>
          </li>
          <li class="nav-item">
            <a href="{{ path('app_logout') }}" class="nav-link active"
  >Logout</a
>
          </li>
          
        </ul>
      </nav>
  <body>
    
        

    {% block body %}{% endblock %}
  </body>
</html>
